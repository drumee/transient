# Provenance — server-runtime

This workspace is a symbol-level extraction, not a directory copy. The source
SHA references the immutable imports recorded in `SOURCE_MANIFEST.md`.

| New responsibility | Source evidence | Source SHA | Intentional Phase 2 boundary/difference |
|---|---|---|---|
| Descriptor discovery and service resolution | `sources/server-team/router/rest/index.js::{registerModules,Acl.getModule}` | `9923d32a117324af1d802ea0909d61aa6d31c7e0` | Keeps `acl/*.json`, public/private selection and `module.method`; removes Team router policy and mutable global singletons. |
| Lazy worker cache and post-authorisation invocation | `sources/server-team/router/rest/index.js::{Acl.run,exec}` | `9923d32a117324af1d802ea0909d61aa6d31c7e0` | Preserves constructor shape `new WorkerClass({ session, permission })`; a host may inject a generic runtime seam such as `pluginResolver`; returns the worker method result for the isolated test gateway instead of using Team `output`/`exception` objects. |
| Bootstrap Workers (`plugin`, `authn`) | `sources/server-team/service/bootstrap.js::{plugin,authn}` and `acl/bootstrap.json::authn` | `9923d32a117324af1d802ea0909d61aa6d31c7e0` | `service/bootstrap.js::BootstrapWorker` preserves plugin resolution and extracts transport-only OTAK issuance. Historical `scope: hub` intentionally becomes `scope: domain`, while `src: anyone` and `fast_check: public-api` remain. The fast path creates no Domain business privilege, Hub or MFS dependency. |
| Public ACL fast path | `sources/server-core/lib/acl.js::{check_preprocess,fast_check,check_env}` | `bf7c396b14614f247507f771f72e98184ed931b4` | Supports the approved `public-api` fast path without creating SQL/MFS dependencies. Both the historical nested `preproc.fast_check` and Phase 3 descriptor-level `fast_check` are accepted at this temporary seam. |
| Permission conversion | `sources/server-essentials/lib/lex/{permission,privilege}.js` | `b9460ba442c9962471a592c49ce36b01e0327ff5` | Callers inject the current Essentials converter. The runtime does not copy numeric permission tables or preserve the historical 1.3.1 mapping. |
| Frontend plugin resolver | `sources/server-team/service/bootstrap.js::plugin` | `9923d32a117324af1d802ea0909d61aa6d31c7e0` | Preserves logical name → `index.json` → `{ path }`; roots/public prefix are explicit instead of read from Team `sysEnv()`. |
| JSON HTTP request body | `sources/ui-essentials/socket/service.js::postService` and historical REST service envelope | `f076a0312273f429b86e60156ecf261dfd7804d1` | `lib/http.js::{requestInput,createServiceServer}` accepts bounded JSON POST input in addition to the Phase 2 query path. It remains a generic adapter and returns the same `{ status, data }` envelope; it has no module-specific branch. |
| Yellow Page login service | `sources/server-team/service/yp.js::login` and `sources/server-core/lib/session.js::signin` | `9923d32a117324af1d802ea0909d61aa6d31c7e0`; `bf7c396b14614f247507f771f72e98184ed931b4` | `service/yp.js::YellowPageWorker.signin` retains only credential forwarding to the session abstraction. Team origin checks, redirect/request-device behavior, counters and product policy remain outside the runtime. |
| Session principal, signed-in state, regsid allocation and OTAK | `sources/server-core/lib/{input,output,session}.js::{validCookie,_authorization,authorization,cookie,_initUser}`; `sources/server-essentials/lib/lex/constants.js::ID_NOBODY`; `sources/server-team/service/bootstrap.js::authn` | `bf7c396b14614f247507f771f72e98184ed931b4`; `b9460ba442c9962471a592c49ce36b01e0327ff5`; `9923d32a117324af1d802ea0909d61aa6d31c7e0` | `lib/session.js` separates provisioned nobody, guest, system, regular and pending-OTP principals from the historical `signed_in` authorization state. It allocates a missing persisted `regsid` server-side with the provisioned nobody principal, repairs only legacy `NULL` UID rows and obtains an opaque 22-character OTAK through `authn_store`. On a newly issued/replaced session it also retains source `Output.cookie()`'s selected-session response header so an allowlisted external transport can privately continue with the historical x-param bridge. It never exposes regsid as a WebSocket credential or trusts a client identity header. |
| Historical HTTP session authorization bridge | `sources/server-core/lib/input.js::{_parseHeader,_authorization,authorization}` and `sources/ui-essentials/socket/utils.js::makeHeaders` | `bf7c396b14614f247507f771f72e98184ed931b4`; `f076a0312273f429b86e60156ecf261dfd7804d1` | `lib/input.js::sessionAuthorization` extracts the actual source wire pair `x-param-keysel: regsid` / `x-param-regsid`, while `SessionManager.fromRequest` resolves it against the persisted context. The target rejects cookie/header conflicts and unknown/malformed values rather than historical silent cookie precedence, and excludes Hub/DMZ key selectors. It does not invent Basic/Bearer/JWT semantics or add SQL; integration proves cross-site CORS, OTAK and socket continuity. |
| Narrow Yellow Page store | `sources/schemas/yellow_page/procedures/session/{session_signin,session_check_cookie,authorization/authn_store}.sql`, `tables/{authn,cookie,sys_conf}.sql` | `cb838e255600a4ec3797dc7ac13659ad9d187421` | `lib/yellow-page-store.js::{signin,ensureSession,resolveSessionContext,storeAuthn,resolveOtak}` retains the source-derived cookie/authn closure. `session_ensure` reads only provisioned `nobody_id`/`guest_id` identity configuration and narrows `session_check_cookie` by excluding MFS tokens, organisation/support, Hub, DMZ, profile and guest-policy behavior. The target adds a 60-second OTAK expiry and cleanup before one-shot socket binding; this is an intentional transport hardening difference, not a historical table-shape claim. |
| Domain ACL scope | `sources/server-core/lib/acl.js::check_domain` and `sources/schemas/yellow_page/procedures/domain/permission.sql::domain_permission` | `bf7c396b14614f247507f771f72e98184ed931b4`; `cb838e255600a4ec3797dc7ac13659ad9d187421` | `lib/{permission,domain-authorizer}.js` dispatches `scope: "domain"` directly to the historical `domain_permission` function before any historical Hub/MFS environment precondition. Hub scope is not activated or emulated. |
| Current Essentials SQL-function adaptation | `sources/server-essentials/lib/mariadb.js::await_func` | `b9460ba442c9962471a592c49ce36b01e0327ff5` | The current API returns a one-field SELECT row for SQL functions. `scalarFunctionValue` adapts that generic row shape only at the runtime store boundary; it does not change `server-essentials` or preserve a Team-specific result format. |
| WebSocket router, OTAK handshake and Origin policy | `sources/server-team/router/push/index.js::{create_connection,authorization,bindSocket,routeDownStream,routeUpstream,updateConnectionsState}` and `server-core/lib/input.js::_authorization` | `9923d32a117324af1d802ea0909d61aa6d31c7e0`; `bf7c396b14614f247507f771f72e98184ed931b4` | `lib/websocket-router.js` preserves `service`, OTAK-to-session binding, targeted Redis, `sys.hello`/`sys.ping`/`sys.keepalive`, cleanup and watchdog. A localized Origin allowlist is added as defense in depth; pre-Upgrade rejections are HTTP 400/401/403. Cookie/regsid is ignored as WebSocket authentication authority. Team presence, conference, Hub/room, Page hash and Wm branches are removed. |
| Generic module push API | `sources/server-essentials/lib/redis-store.js::sendData` and its use from `sources/server-team/router/push/index.js` | `b9460ba442c9962471a592c49ce36b01e0327ff5`; `9923d32a117324af1d802ea0909d61aa6d31c7e0` | `lib/push-bus.js` resolves a caller session to socket recipients then invokes the current generic RedisStore historical `{source,dest,payload}` transport. It has no local-socket producer shortcut, raw Redis module API, broadcast policy, Hub or Team service semantics. |
| Socket/session store seam | `sources/schemas/yellow_page/procedures/session/{socket_bind,socket_free,socket_get,socket_refresh}.sql` | `cb838e255600a4ec3797dc7ac13659ad9d187421` | `YellowPageStore::{resolveOtak,bindSocket,socketGet,freeSocket,refreshSockets,socketRecipients}` keeps historical OTAK resolution/one-use binding semantics in the runtime-owned Phase 4.4 schema closure. DMZ, quota/profile, conference and Hub joins are excluded. |

The pending database-backed portions of `sources/server-core/lib/acl.js` are
explicitly deferred. They are not emulated by this workspace.
