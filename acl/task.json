{
  "services": {
    "list": {
      "doc": "List tasks scoped to a folder node, ordered by Kanban column then rank. Returns all statuses for that folder.",
      "scope": "hub",
      "permission": {
        "src": "read"
      },
      "params": {
        "nid": {
          "type": "string",
          "required": false,
          "doc": "Folder node id to scope to. Absent/null returns legacy unscoped (nid IS NULL) tasks."
        },
        "include_unscoped": {
          "type": "number",
          "required": false,
          "default": 0,
          "doc": "When 1 (workspace-root view), also include legacy tasks with no folder (nid IS NULL)."
        }
      },
      "returns": {
        "type": "array",
        "doc": "Array of task objects ordered by status column then rank",
        "items": {
          "type": "object",
          "properties": {
            "id": { "type": "string", "doc": "Task ID" },
            "title": { "type": "string", "doc": "Task title" },
            "description": { "type": "string", "doc": "Task description (long text), null if not set" },
            "status": { "type": "string", "doc": "Kanban column: todo | in_progress | to_review | complete" },
            "priority": { "type": "string", "doc": "Priority: low | medium | high | urgent" },
            "due_date": { "type": "string", "doc": "Due date (YYYY-MM-DD), null if not set" },
            "created_by": { "type": "string", "doc": "UID of the user who created the task" },
            "nid": { "type": "string", "doc": "Folder node id the task belongs to, null if workspace-level (legacy)" },
            "assignee_uids": { "type": "string", "doc": "Comma-separated list of assignee UIDs, null if unassigned" },
            "rank": { "type": "number", "doc": "Position within the status column" },
            "ctime": { "type": "number", "doc": "Unix timestamp of creation" },
            "mtime": { "type": "number", "doc": "Unix timestamp of last update" },
            "label_ids": { "type": "string", "doc": "Comma-separated list of label IDs attached to the task, null if none" }
          }
        }
      }
    },
    "create": {
      "doc": "Create a new task scoped to a folder node. The task is placed at the bottom of the target status column within that folder.",
      "scope": "hub",
      "permission": {
        "src": "write"
      },
      "params": {
        "title": {
          "type": "string",
          "required": true,
          "doc": "Task title"
        },
        "description": {
          "type": "string",
          "required": false,
          "doc": "Long-form task description. Optional."
        },
        "status": {
          "type": "string",
          "required": false,
          "default": "todo",
          "enum": ["todo", "in_progress", "to_review", "complete"],
          "doc": "Initial Kanban column. Defaults to todo."
        },
        "priority": {
          "type": "string",
          "required": false,
          "default": "medium",
          "enum": ["low", "medium", "high", "urgent"],
          "doc": "Task priority. Defaults to medium."
        },
        "due_date": {
          "type": "string",
          "required": false,
          "doc": "Due date in YYYY-MM-DD format. Optional."
        },
        "nid": {
          "type": "string",
          "required": false,
          "doc": "Folder node id the task belongs to. Absent/null = workspace-level."
        },
        "assignee_uids": {
          "type": "array",
          "required": false,
          "doc": "UIDs of the users to assign. Optional. Each must be a valid drumate.",
          "items": { "type": "string" }
        },
        "assignee_uid": {
          "type": "string",
          "required": false,
          "doc": "Deprecated single-assignee form (use assignee_uids). Still accepted."
        },
        "mention_uids": {
          "type": "array",
          "required": false,
          "doc": "UIDs @-mentioned in the description. Server notifies each (excluding self).",
          "items": { "type": "string" }
        }
      },
      "returns": {
        "type": "object",
        "doc": "Newly created task object"
      },
      "errors": [
        { "code": "INVALID_ASSIGNEE", "message": "assignee_uid does not reference a valid user", "http_status": 400 }
      ]
    },
    "update": {
      "doc": "Update a task's title, description, priority and/or due date. Title / description / priority: omit to keep existing value. due_date is always written through (pass null to clear).",
      "scope": "hub",
      "permission": {
        "src": "write"
      },
      "params": {
        "id": {
          "type": "string",
          "required": true,
          "doc": "Task ID to update"
        },
        "title": {
          "type": "string",
          "required": false,
          "doc": "New task title. Omit to keep existing value."
        },
        "description": {
          "type": "string",
          "required": false,
          "doc": "New task description. Omit to keep existing value; pass empty string to clear."
        },
        "priority": {
          "type": "string",
          "required": false,
          "enum": ["low", "medium", "high", "urgent"],
          "doc": "New priority. Omit to keep existing value."
        },
        "due_date": {
          "type": "string",
          "required": false,
          "doc": "New due date in YYYY-MM-DD format. Pass null to clear."
        },
        "mention_uids": {
          "type": "array",
          "required": false,
          "doc": "UIDs newly @-mentioned in this edit. Server notifies each (excluding self).",
          "items": { "type": "string" }
        }
      },
      "returns": {
        "type": "object",
        "doc": "Updated task object"
      },
      "errors": [
        { "code": "TASK_NOT_FOUND", "message": "No task found with the given ID", "http_status": 404 },
        { "code": "INVALID_PRIORITY", "message": "priority must be one of: low, medium, high, urgent", "http_status": 400 }
      ]
    },
    "update_status": {
      "doc": "Move a task to a different Kanban column. The task is placed at the bottom of the destination column.",
      "scope": "hub",
      "permission": {
        "src": "write"
      },
      "params": {
        "id": {
          "type": "string",
          "required": true,
          "doc": "Task ID to move"
        },
        "status": {
          "type": "string",
          "required": true,
          "enum": ["todo", "in_progress", "to_review", "complete"],
          "doc": "Destination Kanban column"
        }
      },
      "returns": {
        "type": "object",
        "doc": "Updated task object with new status and rank"
      },
      "errors": [
        { "code": "TASK_NOT_FOUND", "message": "No task found with the given ID", "http_status": 404 },
        { "code": "INVALID_STATUS", "message": "status must be one of: todo, in_progress, to_review, complete", "http_status": 400 }
      ]
    },
    "update_assignee": {
      "doc": "Replace a task's assignee set (multi-assignee). Pass the full new set in assignee_uids; an empty array clears all assignees.",
      "scope": "hub",
      "permission": {
        "src": "write"
      },
      "params": {
        "id": {
          "type": "string",
          "required": true,
          "doc": "Task ID"
        },
        "assignee_uids": {
          "type": "array",
          "required": false,
          "doc": "Full new set of assignee UIDs. [] clears all. Each must be a valid drumate.",
          "items": { "type": "string" }
        },
        "assignee_uid": {
          "type": "string",
          "required": false,
          "doc": "Deprecated single-assignee form (use assignee_uids). Still accepted; null unassigns."
        }
      },
      "returns": {
        "type": "object",
        "doc": "Updated task object"
      },
      "errors": [
        { "code": "TASK_NOT_FOUND", "message": "No task found with the given ID", "http_status": 404 },
        { "code": "INVALID_ASSIGNEE", "message": "assignee_uid does not reference a valid user", "http_status": 400 }
      ]
    },
    "delete": {
      "doc": "Delete a task permanently. All linked file and label associations are removed.",
      "scope": "hub",
      "permission": {
        "src": "write"
      },
      "params": {
        "id": {
          "type": "string",
          "required": true,
          "doc": "Task ID to delete"
        }
      },
      "returns": {
        "type": "object",
        "doc": "Deleted task ID and affected row count",
        "properties": {
          "id": { "type": "string", "doc": "Deleted task ID" },
          "affected": { "type": "number", "doc": "Number of rows deleted (1 if found, 0 if not found)" }
        }
      }
    },
    "link_file": {
      "doc": "Link a file (media node) to a task. Idempotent — linking the same file twice has no effect. Returns the full updated list of files linked to the task.",
      "scope": "hub",
      "permission": {
        "src": "write"
      },
      "params": {
        "task_id": {
          "type": "string",
          "required": true,
          "doc": "Task ID to link the file to"
        },
        "file_nid": {
          "type": "string",
          "required": true,
          "doc": "Media node ID of the file to link"
        }
      },
      "returns": {
        "type": "array",
        "doc": "Updated list of all files linked to the task, including media metadata",
        "items": {
          "type": "object",
          "properties": {
            "task_id": { "type": "string", "doc": "Task ID" },
            "file_nid": { "type": "string", "doc": "File media node ID" },
            "linked_by": { "type": "string", "doc": "UID of the user who linked the file" },
            "ctime": { "type": "number", "doc": "Unix timestamp of when the file was linked" },
            "filename": { "type": "string", "doc": "File display name (user_filename from media)" },
            "category": { "type": "string", "doc": "File category (document, image, etc.)" },
            "extension": { "type": "string", "doc": "File extension" },
            "filesize": { "type": "number", "doc": "File size in bytes" }
          }
        }
      }
    },
    "unlink_file": {
      "doc": "Remove the link between a file and a task. The file itself is not deleted from the hub.",
      "scope": "hub",
      "permission": {
        "src": "write"
      },
      "params": {
        "task_id": {
          "type": "string",
          "required": true,
          "doc": "Task ID to unlink the file from"
        },
        "file_nid": {
          "type": "string",
          "required": true,
          "doc": "Media node ID of the file to unlink"
        }
      },
      "returns": {
        "type": "object",
        "doc": "Confirmation of the unlinked pair",
        "properties": {
          "task_id": { "type": "string", "doc": "Task ID" },
          "file_nid": { "type": "string", "doc": "File media node ID" },
          "affected": { "type": "number", "doc": "1 if removed, 0 if link did not exist" }
        }
      }
    },
    "get_linked_files": {
      "doc": "Get all files linked to a task, including file metadata from the media table.",
      "scope": "hub",
      "permission": {
        "src": "read"
      },
      "params": {
        "task_id": {
          "type": "string",
          "required": true,
          "doc": "Task ID to get linked files for"
        }
      },
      "returns": {
        "type": "array",
        "doc": "List of linked file objects with media metadata"
      }
    },
    "link_label": {
      "doc": "Attach a label to a task. Idempotent — linking the same label twice has no effect. Returns the updated list of labels on the task.",
      "scope": "hub",
      "permission": {
        "src": "write"
      },
      "params": {
        "task_id":  { "type": "string", "required": true, "doc": "Task ID" },
        "label_id": { "type": "string", "required": true, "doc": "Label ID" }
      },
      "returns": {
        "type": "array",
        "doc": "Updated list of labels attached to the task",
        "items": {
          "type": "object",
          "properties": {
            "task_id":  { "type": "string", "doc": "Task ID" },
            "label_id": { "type": "string", "doc": "Label ID" },
            "ctime":    { "type": "number", "doc": "Unix timestamp of when the label was linked" },
            "name":     { "type": "string", "doc": "Label name" },
            "color":    { "type": "string", "doc": "Label color (hex with leading #)" }
          }
        }
      }
    },
    "unlink_label": {
      "doc": "Remove a label from a task. The label itself is not deleted from the hub.",
      "scope": "hub",
      "permission": {
        "src": "write"
      },
      "params": {
        "task_id":  { "type": "string", "required": true, "doc": "Task ID" },
        "label_id": { "type": "string", "required": true, "doc": "Label ID" }
      },
      "returns": {
        "type": "object",
        "doc": "Confirmation of the unlinked pair",
        "properties": {
          "task_id":  { "type": "string" },
          "label_id": { "type": "string" },
          "affected": { "type": "number" }
        }
      }
    },
    "get_labels": {
      "doc": "Get all labels attached to a task.",
      "scope": "hub",
      "permission": {
        "src": "read"
      },
      "params": {
        "task_id": { "type": "string", "required": true, "doc": "Task ID" }
      },
      "returns": {
        "type": "array",
        "doc": "List of labels attached to the task"
      }
    },
    "search_files": {
      "doc": "Search media files in the current hub that can be linked to a task. Results are filtered by the caller's read-permission. If task_id is provided, files already linked to that task are excluded.",
      "scope": "hub",
      "permission": {
        "src": "read"
      },
      "params": {
        "pattern": { "type": "string", "required": true, "minLength": 1, "doc": "Filename substring" },
        "task_id": { "type": "string", "required": false, "doc": "Exclude files already linked to this task" },
        "page":    { "type": "number", "required": false, "default": 1, "min": 1, "doc": "Page number for pagination" }
      },
      "returns": {
        "type": "array",
        "doc": "Array of media items matching the search pattern",
        "items": {
          "type": "object",
          "properties": {
            "nid":      { "type": "string", "doc": "Media node ID" },
            "filename": { "type": "string", "doc": "Display filename" },
            "ext":      { "type": "string", "doc": "File extension" },
            "category": { "type": "string", "doc": "File category" },
            "filesize": { "type": "number", "doc": "File size in bytes" },
            "mtime":    { "type": "number", "doc": "Unix timestamp of last upload" },
            "score":    { "type": "number", "doc": "Match score (higher = better)" }
          }
        }
      }
    }
  },
  "modules": {
    "private": "service/private/task"
  }
}
