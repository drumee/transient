<%= renderer.include('block/hello.tpl') %>
<%= renderer.include('block/system-message.tpl', {
    text: lex._hub_invite_link_message.format(inviter_name, workspace_name)
  })
%>

<%= renderer.include('block/link.tpl', {link: link, label: lex._hub_invite_open}) %>

<%= renderer.include('block/system-message.tpl', {
    text: lex._hub_invite_link_expiry.format(expiry_days)
  })
%>

<%= renderer.include('block/signature.tpl') %>
